# Continuous integration: lint, import check, and test suite.

name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  # ── Lint with ruff ──────────────────────────────────────────────────────
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install ruff
        run: pip install ruff
      - name: Run ruff check
        run: ruff check hub/ remote/ tests/
      - name: Run ruff format check
        run: ruff format --check hub/ remote/ tests/

  # ── Verify dependencies install and the app imports cleanly ────────────
  build:
    name: Install & import check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r hub/requirements.txt -r remote/requirements.txt
      - name: Byte-compile and syntax-check sources
        run: |
          python -m compileall -q hub/
          python -m compileall -q remote/

  # ── Test suite ──────────────────────────────────────────────────────────
  test:
    name: Tests (pytest)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r hub/requirements.txt -r remote/requirements.txt
          pip install pytest
      - name: Run test suite
        run: pytest
